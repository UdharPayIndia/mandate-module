<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="android.text.TextUtils"/>
        <import type="android.view.View"/>
        <variable
            name="vm"
            type="com.rocketpay.mandate.feature.product.presentation.ui.order.list.viewmodel.ItemProductOrderListVM"/>
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="@dimen/rp_size_16"
        android:paddingVertical="@dimen/rp_size_12"
        android:orientation="horizontal"
        android:background="?attr/selectableItemBackground"
        android:backgroundTint="@color/rp_grey_6"
        android:onClick="@{() -> vm.onItemClick()}">

        <ImageView
            android:id="@+id/iv_logo"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@{vm.iconDrawable}"
            tools:src="@drawable/rp_ic_super_key"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/rp_size_16"
            android:layout_marginEnd="@dimen/rp_size_16"
            android:orientation="vertical"
            app:layout_constraintStart_toEndOf="@+id/iv_logo"
            app:layout_constraintEnd_toStartOf="@+id/lyt_status"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent">

            <TextView
                android:textAppearance="@style/rp_body_regular_medium"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="@{vm.titleText}"
                tools:text="Rahulkumar" />

            <TextView
                android:layout_marginTop="@dimen/rp_size_2"
                android:textAppearance="@style/rp_body_small"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="@{vm.createdAtText}"
                tools:text="1000 due in 13 days" />

        </LinearLayout>

        <LinearLayout
            android:id="@+id/lyt_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="end"
            android:orientation="vertical"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent">

            <TextView
                android:textAppearance="@style/rp_title"
                android:paddingStart="@dimen/rp_size_8"
                android:paddingEnd="@dimen/rp_size_8"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:visibility="@{TextUtils.isEmpty(vm.amountText) ? View.GONE : View.VISIBLE}"
                android:text="@{vm.amountText}"
                android:textColor="@color/rp_grey_1"
                android:gravity="center"
                tools:text="Finished" />

            <TextView
                android:layout_marginTop="@dimen/rp_size_2"
                android:textAppearance="@style/rp_body_small"
                android:paddingStart="@dimen/rp_size_8"
                android:paddingEnd="@dimen/rp_size_8"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{vm.statusText}"
                tools:textColor="@color/rp_green_2"
                android:gravity="center"
                android:textColor="@{vm.statusTextColor}"
                tools:text="Finished" />

        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>


</layout>